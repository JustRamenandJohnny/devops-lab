name: CI Pipeline
on: [push, pull_request]  # Запуск при пуше и PR

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      # Загружаем код
      - uses: actions/checkout@v2
      
      # Устанавливаем Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '18'
      
      
      - name: Build server
        run: |
          cd server
          npm install
          npm run build  # Если есть скрипт build в package.json
      
      
      - name: Test server
        run: |
          cd server
          npm test
      
      
      - name: Build client
        run: |
          cd client
          npm install
          npm run build
      
      
      - name: Test client
        run: |
          cd client
          npm test